const glow=document.querySelector('.cursor-glow');addEventListener('mousemove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const stars=document.querySelector('.stars');if(stars){for(let i=0;i<85;i++){const s=document.createElement('i');s.style.cssText='position:absolute;width:'+(Math.random()>0.88?2:1)+'px;height:'+(Math.random()>0.88?2:1)+'px;background:white;border-radius:50%;left:'+Math.random()*100+'%;top:'+Math.random()*100+'%;opacity:'+(Math.random()*.55+.08);stars.appendChild(s)}}const nav=document.querySelector('.nav');addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>90),{passive:true});const menu=document.querySelector('.menu'),mobileNav=document.querySelector('.nav nav');menu.addEventListener('click',()=>{mobileNav.classList.toggle('mobile-open');menu.setAttribute('aria-expanded',mobileNav.classList.contains('mobile-open'))});mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileNav.classList.remove('mobile-open')));